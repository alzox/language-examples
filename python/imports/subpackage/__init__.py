from . import c

"""
This subpackage then allows what can be imported from the top-level package.

i.e. if only from c import [function], then the top-level could only do from .subpackage import [function]
"""
